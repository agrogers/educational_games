<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="lonely_s_game.GameTemplate" owl="1">
        <div class="o_action_manager o_content p-5 bg-light h-100 overflow-auto">
            <div class="card shadow-sm mx-auto" style="max-width: 800px;">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">The "Lonely S" Challenge</h3>
                    <p class="mb-0">Fix the missing 's' in each sentence!</p>
                </div>
                <div class="card-body">
                    <div t-foreach="state.sentences" t-as="item" t-key="item.id" class="mb-4 p-3 border-bottom">
                        <p class="fs-5">
                            <span t-esc="item.id"/>. 
                            <span t-attf-class="{{state.checked and 'text-danger fw-bold' or ''}}">
                                <t t-esc="item.text"/>
                            </span>
                        </p>
                        <div class="d-flex align-items-center">
                            <input type="text" 
                                   class="form-control w-50" 
                                   placeholder="Type the corrected word..."
                                   t-model="item.userAnswer"
                                   t-att-disabled="state.checked"/>
                            
                            <span t-if="state.checked" class="ms-3">
                                <t t-if="item.userAnswer.toLowerCase() === item.correctWord.toLowerCase()">
                                    <span class="text-success fs-4">✅</span>
                                </t>
                                <t t-else="">
                                    <span class="text-danger">❌ Correct: <strong t-esc="item.correctWord"/></span>
                                </t>
                            </span>
                        </div>
                    </div>

                    <div class="text-center mt-4">
                        <h4 t-if="state.checked" class="mb-3">
                            Final Score: <t t-esc="state.score"/>%
                        </h4>
                        <button t-if="!state.checked" class="btn btn-lg btn-success" t-on-click="checkAnswers">
                            Check Answers
                        </button>
                        <button t-if="state.checked" class="btn btn-lg btn-outline-primary" t-on-click="resetGame">
                            Try Again
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>